{
  "name": "plantgreen",
  "version": "1.0.0",
  "author": "Glory",
  "license": "ISC",
  "engines": {
    "node": "18.x"
  },
  "scripts": {
    "start": "node backend/app.js",
    "heroku-postbuild": "cd frontend && yarn && yarn build && cd ../backend && yarn"
  },
  "dependencies": {
    "dotenv": "^16.0.3",
    "express": "^4.18.1",
    "yarn": "^1.22.19"
  }
}

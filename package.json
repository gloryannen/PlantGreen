{
  "name": "plantgreen",
  "version": "1.0.0",
  "author": "Glory",
  "license": "ISC",
  "engines": {
    "node": "18.x"
  },
  "scripts": {
    "start": "node backend/app.js",
    "heroku-postbuild": "cd frontend && npm start && npm run build && cd ../backend && npm start"
  },
  "dependencies": {
    "dotenv": "^16.0.3",
    "express": "^4.18.1"
  }
}
